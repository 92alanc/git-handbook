Git commands

git status

git pull
git pull [remote_name] [branch_name]

git add .
git add [file_name]

git commit
git commit -m "[any message]"
git commit -i
git commit -a

git push [remote_name] [branch_name]
git push [remote_name] --delete [branch_name]

git stash
git stash pop
git stash show
git stash show -p
git stash show -p [stash_id]

git init

git remote add [remote_name] [url/ssh]

git checkout .
git checkout [file_name]
git checkout [branch_name]
git checkout -b [new_branch_name]

git reset --hard HEAD

git rebase [branch_name]
git rebase -i [branch_name]
git rebase [commit_id]
git rebase -i [commit_id]

git log

git revert [commit_id]

git branch
git branch -d [branch_name]
git branch -D [branch_name]
git branch -a
git branch --merged

git config --global [item_name] [value]

git clean -f
git clean -df
git clean -xdf

git merge [branch_name]

git diff
git diff [file_name]
git diff [branch_name]

git blame [file_name]

git difftool

git mergetool

git gui

touch [file_name]

rm [file_name]
rm -f [file_name]
rm -r [directory_name]
rm -rf [directory_name]

cat [file_name]